public enum LaunchMode { Automatic, Manual }

public enum LauncherState { Off, SpinningUp, Ready, Empty, DeadBattery }

public enum TargetType { None, Aircraft, Sun, Cloud, Flare }

public enum AircraftType { MI24, SU25 }